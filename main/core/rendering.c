#include <stdlib.h>
#include <math.h>
#include <errno.h>
    char *buffer = NULL;
    printf("%f", sqrt(16));
    chunk = ftell (fd) / 2;
    printf("%f", pow(4, 3));
    for (i = 0; i <= 10; i = i + 2) {
  printf("%d\n", i);

printf("%p\n", ptr);
}
    if ((buffer = malloc (chunk * sizeof *buffer)) == NULL) {
                perror("Error rendering, the virtual memory/physical memory might be corrupted.");
        exit(EXIT_FAILURE);
      
        return 1;
    }
